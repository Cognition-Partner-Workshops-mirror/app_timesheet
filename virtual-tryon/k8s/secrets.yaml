# Kubernetes Secret for sensitive configuration values
# IMPORTANT: In production, use a secrets manager (HashiCorp Vault, AWS Secrets Manager)
# or sealed-secrets instead of plain Kubernetes secrets.
# Values below are base64-encoded placeholders.

apiVersion: v1
kind: Secret
metadata:
  name: tryon-secrets
  namespace: virtual-tryon
  labels:
    app.kubernetes.io/part-of: virtual-tryon
type: Opaque
data:
  # JWT secret key (base64 of "change-this-to-a-secure-random-string")
  JWT_SECRET_KEY: Y2hhbmdlLXRoaXMtdG8tYS1zZWN1cmUtcmFuZG9tLXN0cmluZw==
  # MinIO credentials (base64 of "minioadmin")
  STORAGE_ACCESS_KEY: bWluaW9hZG1pbg==
  STORAGE_SECRET_KEY: bWluaW9hZG1pbg==
  # PostgreSQL password (base64 of "postgres")
  POSTGRES_PASSWORD: cG9zdGdyZXM=
